# Future frontend

The Musimack SEO Toolkit frontend is planned to use React, TypeScript, and Vite in this
monorepo. Frontend initialization, npm dependencies, application code, and build tooling are
deliberately deferred. This foundation batch installs no frontend dependencies.
